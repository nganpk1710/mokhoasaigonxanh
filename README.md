
  # Convert Figma to HTML CSS

  This is a code bundle for Convert Figma to HTML CSS. The original project is available at https://www.figma.com/design/SfIjGdu39UZGiXZ87aTyWq/Convert-Figma-to-HTML-CSS.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  